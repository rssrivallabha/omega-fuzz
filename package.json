{
  "name": "omega-fuzz",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "turbo run build",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "turbo": "^2.0.4",
    "vite": "^8.1.1"
  },
  "packageManager": "npm@10.9.2"
}
